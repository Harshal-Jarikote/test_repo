test:
  override:
    - |
      (if [ "TRUE" ]; then
        echo "Starting the build..."
        sleep 700
      else
        echo "skipping the build (as not triggered by nightly API call)"
      fi;)
  timeout: 705
